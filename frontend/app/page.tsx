import { LandingPage } from "@/components/landing-page";

// The landing proof card reads database-backed Test Mode evidence at request time.
export const dynamic = "force-dynamic";

export default function Landing() {
  return <LandingPage />;
}
